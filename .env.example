# Copy to .env and adjust. Local dev points at a throwaway Postgres.
# In CI and on the deployed preview this is injected as a secret / service binding.
DATABASE_URL=postgresql://postgres@localhost:5433/app?schema=public
PORT=3000
HOST=0.0.0.0
